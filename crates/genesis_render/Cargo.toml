[package]
name = "genesis_render"
version.workspace = true
edition.workspace = true
publish.workspace = true

[lib]

[dependencies]
bevy = { workspace = true }
genesis_core = { path = "../genesis_core" }
glam = "0.30"
