[package]
name = "genesis_climate"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
genesis_core = { path = "../genesis_core" }
serde = { version = "1", features = ["derive"] }
tracing = { version = "0.1" }
